"""
Calculate and print the perimeter of a rectangle from user input (2 * (length + width)).
"""

# Solution:

length = float(input("Enter the Length  "))

width = float(input("Enter the width "))

print (2 * (length + width))